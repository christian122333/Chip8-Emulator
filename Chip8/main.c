#include "cpu.h"
#include "graphics.h"
#include "input.h"



int main(int argc, char **argv) {
    if (!init_graphics()) {
        return 0;
    }
 //   screen_clear();
  //  init_cpu();
    
    while(1){
   //     render();
    //    cycle();
     //   delay(17);  // delay for 17 ms to simulate 60Hz
    }
    
    return 0;
}